function soma(x, y) {
    return x + y
};

//console.log(soma(3, 4));

const multiplicacao = function(x, y) {
    return x * y;
};

//console.log(multiplicacao(3, 4));

const dividir = (x, y) => {
    return x / y;
};


const objeto = () => ({
    nome: 'JC', sobrenome: 'Bombardelli'
});